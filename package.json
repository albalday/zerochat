{
  "name": "zerochat",
  "private": true,
  "version": "6.6.0",
  "description": "ZeroChat - Cliente Web Universal de Chat con soporte multimodelo, razonamiento autónomo y RAG local embebido en un solo archivo",
  "scripts": {
    "build:vendor": "node scripts/build-orama.mjs",
    "build": "npm run build:vendor && python3 bundle.py index.html zerochat.html",
    "test": "node --test tests/test_*.js",
    "test:unit": "node --test --test-skip-pattern='^Browser UI' tests/test_*.js",
    "test:browser": "node tests/test_browser_ui.js"
  },
  "dependencies": {
    "@orama/orama": "3.1.18"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "fake-indexeddb": "6.2.5",
    "playwright": "^1.62.1"
  }
}
