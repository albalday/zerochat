# Host MCP externo

`zerochat_mcp_host.py` descubre directorios `*.mcp`, instala sus productos declarados y enruta MCP stdio por el canal privado con el servidor local. No contiene una lista de servicios por código.
